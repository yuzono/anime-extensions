ext {
    extName = 'Animelib'
    extClass = '.Animelib'
    extVersionCode = 12
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlistutils'))
}
