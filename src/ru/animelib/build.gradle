ext {
    extName = 'Animelib'
    extClass = '.Animelib'
    extVersionCode = 13
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlistutils'))
}
