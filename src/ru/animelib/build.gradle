ext {
    extName = 'Animelib'
    extClass = '.Animelib'
    extVersionCode = 14
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlistutils'))
}
