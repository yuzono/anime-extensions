ext {
    extName = 'AnimeIto'
    extClass = '.AnimeIto'
    themePkg = 'animestream'
    baseUrl = 'https://animei.to'
    overrideVersionCode = 1
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlistutils"))
    implementation(project(":lib:unpacker"))
}
