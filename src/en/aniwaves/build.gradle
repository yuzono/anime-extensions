ext {
    extName = 'AniWaves (Unoriginal)'
    extClass = '.AniWaves'
    extVersionCode = 1
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlistutils"))
}
